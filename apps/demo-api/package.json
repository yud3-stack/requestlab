{
  "name": "@requestlab/demo-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@requestlab/shared": "workspace:*",
    "@requestlab/sdk-node": "workspace:*",
    "dotenv": "^16.6.1",
    "fastify": "^5.5.0"
  }
}
